# הקשר הפרויקט - בריאות טבעית

## מה הפרויקט עושה
אפליקציית ווב חכמה למאגר ידע מהרצאות הרב אשרוב בנושא בריאות טבעית.
- חיפוש נושאים
- שאלות ותשובות עם AI (Gemini)
- פינת "הידעת?" מותאמת אישית
- הקראת תשובות בקול (TTS)

## טכנולוגיות ותלויות
- **שפה:** HTML + CSS + JavaScript (Vanilla)
- **API:** Google Gemini API (AI תשובות)
- **TTS:** Web Speech API (הקראה בעברית)
- **נתונים:** JSON מוטמע בקובץ (הרצאות הרב)
- **אחסון:** localStorage (העדפות משתמש)

## מבנה הפרויקט
```
briut-tivit/
├── index.html          - הקובץ הראשי (כולל HTML, CSS, JS, DATA)
├── PROJECT-CONTEXT.md  - הקובץ הזה
├── .env                - מפתחות API
└── playground.html     - (קובץ ניסיון, לא בשימוש)
```

## מה בנינו עד כה

### ✅ תכונות שעובדות
- **מסך בית** - חיפוש, שאלות, נושאים
- **מנוע חיפוש חכם** - עם מילים נרדפות בעברית
- **צ'אט עם הרב** - שאלות ותשובות מבוססות AI
- **כרטיסי תשובה מעוצבים** - עם ציטוטים, טיפים, מקורות
- **הקראת טקסט (TTS)** - קול גברי בעברית, הדגשת שורה נוכחית
- **פינת "הידעת?"** 💡 - עובדות מעניינות מותאמות אישית
- **ניווט** - בין דפים, כפתור חזרה
- **סטטיסטיקות** - מספר הרצאות ונושאים

### 🚧 בעבודה / בעיות ידועות
- **מסך כניסה (PIN)** - הוסר זמנית כי לא עבד (בעיה בפוקוס אוטומטי)
- **כפתור הגדרות** - קיים אבל לא נבדק לאחרונה

### ❌ מה שניסינו ולא עבד
- תיקון מסך הכניסה עם `type="password"` - לא עבד
- תיקון עם `type="tel"` + CSS masking - גם לא עבד
- החלטה: הסרנו זמנית את מסך הכניסה

## היסטוריה - שיחה 2026-02-15

### 1. תיקון API Key שפג תוקף
**בעיה:** מפתח Gemini API החזיר שגיאה "API key expired"

**פתרון:**
- עדכנו ב-`.env.master`: `AIzaSyDCKZ7XcYHJgNhPjKTXOImVB59iDPPwzqQ`
- עדכנו ב-`index.html` (מוצפן base64): `QUl6YVN5RENLWjdYY1lISmdOaFBqS1RYT0ltVkI1OWlEUFB3enFR`
- עדכנו ב-`.env` של הפרויקט

**Commit:** `133423e` - Update Gemini API key

### 2. תיקון שדות PIN להצגת כוכביות
**בעיה:** שדות הקוד הציגו מספרים במקום כוכביות

**פתרון:**
- שינינו מ-`type="tel"` ל-`type="password"`

**Commit:** `fd24f18` - Fix PIN input security

### 3. ניסיונות לתקן מסך כניסה שלא עבד
**בעיה:** הקלט לא עבר אוטומטית לשדה הבא, Enter לא עבד

**ניסיונות:**
- ניסיון 1: שיפור event listeners ב-JavaScript
- ניסיון 2: שינוי ל-`type="tel"` + CSS `-webkit-text-security: disc`
- שניהם לא עבדו

**החלטה סופית:** הסרנו את מסך הכניסה זמנית

**Commit:** `919fbee` - Remove login screen temporarily

### 4. הוספת פינת "הידעת?" - ניסיון ראשון
**מה עשינו:**
- הוספנו CSS, HTML, JavaScript לפינת "הידעת?"
- לוגיקה לבחירת עובדות אקראיות מההרצאות

**בעיה:** הקוד לא נוסף כמו שצריך והאתר נשבר (לא אפשר ללחוץ על כלום)

**סיבה:** הקריאה ל-`displayDidYouKnow()` נשארה אבל הפונקציה נמחקה → שגיאת JavaScript

**Commit שנכשל:** `b05e8c1` (נשאר ב-Git אבל לא עבד)

### 5. תיקון האתר ששבר
**פתרון:** שחזרנו מגיבוי למצב עובד

**Commit:** `fc57e37` - Fix broken site - restore to working version

### 6. הוספת פינת "הידעת?" - ניסיון שני (הצליח!)
**גישה חדשה:** סקריפט Python שהוסיף בזהירות את כל החלקים

**מה הוספנו:**
- CSS מעוצב עם גרדיאנט
- HTML קומפוננטה
- JavaScript פשוט - בוחר עובדות אקראיות
- כפתור "עובדה נוספת"

**Commit:** `d12519c` - Add Did You Know section (working version)

### 7. שיפור פינת "הידעת?" - התאמה אישית
**בקשה:** להפוך את זה למותאם אישית ומתומצת

**שיפורים:**
- **למידה:** שומר נושאים שהמשתמש שואל עליהם (localStorage)
- **התאמה:** מציג עובדות רלוונטיות לנושאים
- **תמציתיות:** מחלץ משפט אחד מעניין (50-180 תווים) במקום קטע ארוך
- **איכות:** מעדיף עובדות עם מספרים, סטטיסטיקות, מילות הדגשה

**אלגוריתם:**
1. לומד מה המשתמש שואל (שומר במערך `UserTopics.topics`)
2. מסנן chunks: 100-500 תווים
3. ניקוד לפי רלוונטיות (x3), תמציתיות, מספרים, הדגשה
4. בוחר מהTop 5
5. מחלץ את המשפט הכי מעניין

**Commit:** `5bb9fe6` - Improve Did You Know - personalized and concise

## מצב נוכחי

### עובד ✅
- חיפוש ושאלות
- צ'אט עם AI
- TTS (הקראה)
- פינת "הידעת?" - חכמה ומותאמת אישית
- כל הניווט

### לא עובד ⚠️
- מסך כניסה (הוסר זמנית)

### צריך בדיקה 🔧
- כפתור הגדרות (קיים אבל לא בדקנו)

## קבצים חשובים
- `index.html` - **הקובץ הראשי** (4.8MB - כולל את כל הנתונים)
- `.env` - מפתחות API
- `PROJECT-CONTEXT.md` - הקובץ הזה

## פקודות נפוצות
```bash
# פתיחת האתר
start index.html

# Git
git add .
git commit -m "הודעה"
git push

# עדכון PROJECT-CONTEXT
# (תמיד לעדכן בסוף כל שיחה!)
```

## בעיות ידועות

### מסך כניסה לא עובד
**סיבה:** Event listeners לא עובדים טוב עם `type="password"` + focus אוטומטי

**פתרון זמני:** הסרנו את מסך הכניסה (`style="display:none;"`)

**פתרון עתידי אפשרי:**
- להשתמש ב-library חיצונית ל-PIN input
- לבנות מערכת כניסה אחרת לגמרי
- או פשוט להשאיר בלי כניסה

## צעדים הבאים (אופציונלי)

### בעדיפות גבוהה
1. ✅ ~~תיקון API key~~
2. ✅ ~~הוספת פינת "הידעת?"~~
3. ✅ ~~שיפור "הידעת?" להיות מותאמת~~

### בעדיפות בינונית
1. בדיקת כפתור הגדרות
2. החלטה לגבי מסך כניסה (לתקן או להשאיר בלי)

### רעיונות לעתיד
- אפשרות לשמור תשובות מועדפות
- היסטוריית שאלות
- שיתוף תשובות
- מצב לילה

## קישורים חשובים
- **GitHub repo:** https://github.com/Raniv123/briut-tivit
- **Commit אחרון:** `5bb9fe6` - Improve Did You Know

---
📅 **עדכון אחרון:** 2026-02-15 02:15
💬 **שיחה אחרונה:** הוספנו פינת "הידעת?" חכמה ומותאמת אישית
👤 **מפתח:** ניב + Claude Sonnet 4.5
